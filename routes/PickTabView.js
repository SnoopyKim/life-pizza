import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default function PickTabView() {
    return (
        <View>
            <Text>This is PickTabView</Text>
        </View>
    );
}

const styles = StyleSheet.create({});
